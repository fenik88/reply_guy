# X Reply Generator - Environment Variables

# =====================================================
# GROQ API CONFIGURATION (Required)
# =====================================================

# Get your free API key from: https://console.groq.com
# After registration, create an API key and paste it here
GROQ_API_KEY=your_groq_api_key_here

# =====================================================
# MODEL SELECTION (Optional)
# =====================================================

# Default: llama-3.1-8b-instant (fastest, recommended)
# Alternatives:
#   - llama-3.3-70b-versatile (smarter, slower)
#   - mixtral-8x7b-32768 (good balance)
GROQ_MODEL=llama-3.1-8b-instant

# =====================================================
# SERVER CONFIGURATION (Optional)
# =====================================================

# Port for the local server
# Default: 8765
PORT=8765

# Groq API Base URL (don't change unless you know what you're doing)
GROQ_BASE_URL=https://api.groq.com/openai/v1

# =====================================================
# USAGE INSTRUCTIONS
# =====================================================

# macOS/Linux:
#   1. Copy this file: cp .env.example .env
#   2. Edit .env with your API key
#   3. Load variables: source .env
#   4. Run server: python3 python-server.py

# Windows (PowerShell):
#   1. Copy this file to .env
#   2. Edit .env with your API key
#   3. Load: Get-Content .env | ForEach-Object { $var = $_.Split('='); [Environment]::SetEnvironmentVariable($var[0], $var[1]) }
#   4. Run server: python python-server.py

# Windows (CMD):
#   1. Don't use .env file
#   2. Set directly: set GROQ_API_KEY=your_key_here
#   3. Run server: python python-server.py

# =====================================================
# SECURITY NOTE
# =====================================================

# NEVER commit .env file to git!
# .env is already in .gitignore
# Keep your API key secret!